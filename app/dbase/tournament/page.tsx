"use client";
import Link from "next/link";
import {useEffect,useState} from "react";
import "../dbase.css";

type Tournament=Record<string,any>;
const fmt=(v:any)=>v?new Intl.DateTimeFormat("en-NG",{dateStyle:"medium",timeStyle:"short"}).format(new Date(v)):"—";
const inputDate=(v:any)=>{if(!v)return "";const d=new Date(v),p=(n:number)=>String(n).padStart(2,"0");return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`};
const iso=(v:string)=>v?new Date(v).toISOString():"";
function Field({label,value,onChange,type="text"}:{label:string;value:any;onChange:(v:string)=>void;type?:string}){return <label className="admin-field"><span>{label}</span><input type={type} value={value??""} onChange={e=>onChange(e.target.value)}/></label>}
function Select({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}){return <label className="admin-field"><span>{label}</span><select value={value} onChange={e=>onChange(e.target.value)}>{options.map(x=><option key={x}>{x}</option>)}</select></label>}
export default function TournamentAdmin(){const[rows,setRows]=useState<Tournament[]>([]),[edit,setEdit]=useState<Tournament|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(""),[notice,setNotice]=useState("");const load=async()=>{try{setLoading(true);const r=await fetch("/api/admin/tournaments",{cache:"no-store"}),d=await r.json();if(!r.ok)throw Error(d.error);setRows(d.tournaments||[]);setError("")}catch(e:any){setError(e.message||"Unable to load tournaments")}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const save=async()=>{if(!edit)return;const body={...edit,action:edit.id?"edit":"create",starts_at:iso(edit.startsAtLocal),ends_at:iso(edit.endsAtLocal)};const r=await fetch("/api/admin/tournaments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),d=await r.json();if(!r.ok){setError(d.error||"Unable to save tournament");return}setEdit(null);setNotice("Tournament saved successfully.");load()};const setStatus=async(id:string,status:string)=>{const r=await fetch("/api/admin/tournaments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"status",id,status})});if(!r.ok)setError((await r.json()).error||"Unable to update tournament");else{setNotice("Tournament status updated.");load()}};const startNew=()=>{const now=new Date(),end=new Date(now.getTime()+86400000);setEdit({name:"New tournament",description:"",entry_fee_coins:0,entry_fee_gems:0,prize_pool_coins:0,prize_pool_gems:0,max_players:16,participation_reward_coins:1000,participation_reward_gems:0,prize_multiplier:.2,startsAtLocal:inputDate(now),endsAtLocal:inputDate(end),status:"draft"})};return <main className="dbase-app"><aside className="dbase-sidebar"><div className="brand"><div className="brand-mark">♛</div><div><b>LUDO LIVE</b><span>ADMIN CONTROL</span></div></div><nav><div className="nav-wrap"><small>Live Ops</small><Link href="/dbase">◈ Dashboard</Link><Link href="/dbase/missions">✓ Missions</Link><Link href="/dbase/events">◇ Events</Link><Link className="active" href="/dbase/tournament">♛ Tournament</Link></div></nav></aside><section className="dbase-main"><header className="dbase-header"><div className="header-title"><span>LIVE OPS</span><h1>Tournament</h1></div><div className="admin-user"><div className="user-avatar">A</div><div className="user-copy"><b>Administrator</b><span>Tournament control</span></div></div></header><div className="dbase-content"><div className="page-intro"><div><span className="eyebrow">TOURNAMENT CONTROL</span><h2>Current tournaments</h2><p>Configure entry fees, prize pools, player limits, participation rewards and schedules. All amounts are game coins and gems.</p></div><button className="admin-btn primary-btn" onClick={startNew}>＋ Add tournament</button></div>{notice&&<div className="panel"><div className="empty good">✓ {notice}</div></div>}{error&&<div className="panel"><div className="empty bad">{error}</div></div>}{loading?<div className="dbase-loading"><div><div className="loader-ring"/><p>Loading tournaments…</p></div></div>:<div className="panel">{rows.length?rows.map(r=><div className="case-row" key={r.id}><div className="mini-avatar">♛</div><div className="row-main"><b>{r.name}</b><span>{r.description||"No description"} · {r.players_count||0}/{r.max_players||0} players</span><span>🗓 {fmt(r.starts_at)} → {fmt(r.ends_at)}</span></div><div className="row-meta"><b>🪙 {Number(r.prize_pool_coins||0).toLocaleString()} · 💎 {Number(r.prize_pool_gems||0).toLocaleString()}</b><span>{r.status}</span></div><select className="admin-select" value={r.status} onChange={e=>setStatus(r.id,e.target.value)}><option>draft</option><option>open</option><option>live</option><option>finished</option><option>cancelled</option></select><button className="admin-btn" onClick={()=>setEdit({...r,startsAtLocal:inputDate(r.starts_at),endsAtLocal:inputDate(r.ends_at)})}>Edit</button></div>):<div className="empty">No tournaments configured.</div>}</div>}{edit&&<div className="editor-card"><div className="editor-head"><b>{edit.id?"Edit tournament":"Add tournament"}</b><button className="admin-btn" onClick={()=>setEdit(null)}>✕</button></div><Field label="Tournament name" value={edit.name} onChange={v=>setEdit({...edit,name:v})}/><Field label="Description" value={edit.description} onChange={v=>setEdit({...edit,description:v})}/><div className="form-grid"><Field label="Entry coins" type="number" value={edit.entry_fee_coins} onChange={v=>setEdit({...edit,entry_fee_coins:Number(v)})}/><Field label="Entry gems" type="number" value={edit.entry_fee_gems} onChange={v=>setEdit({...edit,entry_fee_gems:Number(v)})}/><Field label="Prize pool coins" type="number" value={edit.prize_pool_coins} onChange={v=>setEdit({...edit,prize_pool_coins:Number(v)})}/><Field label="Prize pool gems" type="number" value={edit.prize_pool_gems} onChange={v=>setEdit({...edit,prize_pool_gems:Number(v)})}/><Field label="Maximum players" type="number" value={edit.max_players} onChange={v=>setEdit({...edit,max_players:Number(v)})}/><Field label="Participation coins" type="number" value={edit.participation_reward_coins} onChange={v=>setEdit({...edit,participation_reward_coins:Number(v)})}/><Field label="Prize multiplier" type="number" value={edit.prize_multiplier} onChange={v=>setEdit({...edit,prize_multiplier:Number(v)})}/><Field label="Start date & time" type="datetime-local" value={edit.startsAtLocal} onChange={v=>setEdit({...edit,startsAtLocal:v})}/><Field label="End date & time" type="datetime-local" value={edit.endsAtLocal} onChange={v=>setEdit({...edit,endsAtLocal:v})}/></div><div className="empty">Schedule preview: <b>{fmt(edit.startsAtLocal)}</b> → <b>{fmt(edit.endsAtLocal)}</b></div><button className="admin-btn primary-btn full" onClick={save}>Save tournament</button></div>}</div></section></main>}
