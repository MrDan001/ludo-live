"use client";
import {useEffect,useMemo,useState} from "react";
import Link from "next/link";
import "../dbase.css";

type User=Record<string,any>;
const n=(v:any)=>Number(v||0).toLocaleString();
export default function FinancePage(){
 const [users,setUsers]=useState<User[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");
 useEffect(()=>{fetch("/api/admin",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to load finance");setUsers(d.users||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);
 const total=useMemo(()=>users.reduce((s,u)=>s+Number(u.coins??u.coin_balance??u.wallet?.coins??0),0),[users]);
 const gems=useMemo(()=>users.reduce((s,u)=>s+Number(u.gems??u.gem_balance??u.wallet?.gems??0),0),[users]);
 return <main className="dbase-app"><aside className="dbase-sidebar"><div className="brand"><div className="brand-mark">♛</div><div><b>LUDO LIVE</b><span>ADMIN CONTROL</span></div></div><nav><div className="nav-wrap"><small>Management</small><Link href="/dbase">◈ Dashboard</Link><Link href="/dbase/economy">◈ Economy</Link><Link className="active" href="/dbase/finance">₦ Finance</Link><Link href="/dbase/shop">▣ Shop</Link><Link href="/dbase/tournament">♛ Tournament</Link></div></nav></aside><section className="dbase-main"><header className="dbase-header"><div className="header-title"><span>LIVE CONTROL</span><h1>Finance</h1></div><div className="admin-user"><div className="user-avatar">A</div><div className="user-copy"><b>Administrator</b><span>Financial overview</span></div></div></header><div className="dbase-content"><div className="page-intro"><div><span className="eyebrow">FINANCIAL CONTROL</span><h2>Finance center</h2><p>A clean financial overview for the live game economy. Player wallet balances are shown separately from real-money payment records so Admin never mistakes virtual currency for cash revenue.</p></div><div className="live-pill"><span/> LIVE DATA</div></div>{error?<div className="panel"><div className="empty">{error}</div></div>:loading?<div className="dbase-loading"><div><div className="loader-ring"/><p>Loading finance…</p></div></div>:<><div className="stat-grid"><div className="stat-card"><div className="stat-icon">₦</div><div><span>Payment records</span><strong>—</strong><small>Connects only to verified payment data</small></div></div><div className="stat-card"><div className="stat-icon">🪙</div><div><span>Virtual coin value</span><strong>{n(total)}</strong><small>Player wallet units, not cash</small></div></div><div className="stat-card"><div className="stat-icon">💎</div><div><span>Virtual gems</span><strong>{n(gems)}</strong><small>Player wallet units, not cash</small></div></div><div className="stat-card"><div className="stat-icon">♟</div><div><span>Wallet accounts</span><strong>{n(users.length)}</strong><small>Accounts included in snapshot</small></div></div></div><div className="panel-grid lower"><div className="panel"><div className="panel-head"><div><span>REAL MONEY</span><h3>Payment ledger</h3></div></div><div className="empty">No dedicated real-money transaction feed is exposed by the current Admin API. This panel intentionally does not invent revenue numbers.</div></div><div className="panel"><div className="panel-head"><div><span>ACCOUNTING SAFETY</span><h3>Currency separation</h3></div></div><div className="activity"><div className="activity-dot">₦</div><div><b>Real-money payments</b><span>Must come from verified Paystack/payment records.</span></div></div><div className="activity"><div className="activity-dot">🪙</div><div><b>Coins</b><span>Virtual game currency stored in player wallets.</span></div></div><div className="activity"><div className="activity-dot">💎</div><div><b>Gems</b><span>Virtual premium currency stored in player wallets.</span></div></div></div></div></>}</div></section></main>;
}