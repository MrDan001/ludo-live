"use client";

import { useCallback, useEffect, useMemo, useRef, useState, Suspense } from "react";
import { useSearchParams } from "next/navigation";
import { io, type Socket } from "socket.io-client";
import LudoBoard, { type BoardThemeId, type DemoToken } from "../_components/LudoBoardMultiplayer";
import DemoDice from "../_components/DemoDice";
import ChatVoice from "../_components/ChatVoice";
import { canMove, nextProgress, FINISH_PROGRESS, type DiceValue } from "../../lib/ludoEngine";
import { playerColorsForSeats } from "../../lib/ludoRules";

export const dynamic = "force-dynamic";

type Color = "red" | "yellow" | "green" | "blue";
type Player = { playerId: string; name: string; seat: number; colors?: Color[]; level?: number; avatar?: string };
type TokenMap = Record<string, Record<string, { position: number }>>;
type GameState = { status: string; currentPlayerId: string | null; dice: DiceValue | null; pendingMove: DiceValue | null; players: Player[]; tokens: TokenMap; winnerId?: string | null; stateRevision?: number };

const COLORS: Color[] = ["red", "yellow", "green", "blue"];
const FINISH = FINISH_PROGRESS;
const initialTokens = (): DemoToken[] => COLORS.flatMap((color) => Array.from({ length: 4 }, (_, id) => ({ color, id, position: 0, state: "yard" as const })));
const normalizeTokens = (serverTokens: TokenMap): DemoToken[] => COLORS.flatMap((color) => Array.from({ length: 4 }, (_, id) => { const raw = serverTokens?.[color]?.[String(id)]?.position; const position = typeof raw === "number" && Number.isFinite(raw) ? raw : 0; return { color, id, position, state: position === 0 ? ("yard" as const) : position === FINISH ? ("finished" as const) : position > 51 ? ("home" as const) : ("track" as const) }; }));

function PlayerAvatar({ src, fallback }: { src?: string; fallback: string }) { if (src && (src.startsWith("http") || src.startsWith("/") || src.startsWith("data:"))) return <img src={src} alt="Avatar" className="ll-avatar-img-element" />; return <span>{src || fallback}</span>; }
const QUICK_REACTIONS = ["👋 Hi!", "😂 LOL", "🔥 Nice!", "👍 Good move", "🏆 GG", "😜"];

function GameContent() {
  const params = useSearchParams();
  const [theme] = useState<BoardThemeId>("classic");
  const [socket, setSocket] = useState<Socket | null>(null);
  const [me, setMe] = useState("");
  const [myName, setMyName] = useState("");
  const [myAvatarUrl, setMyAvatarUrl] = useState("");
  const [myLevel, setMyLevel] = useState(1);
  const [game, setGame] = useState<GameState | null>(null);
  const [tokens, setTokens] = useState<DemoToken[]>(initialTokens);
  const [roll, setRoll] = useState<DiceValue>(6);
  const [pending, setPending] = useState<DiceValue | null>(null);
  const [remoteRolling, setRemoteRolling] = useState(false);
  const [animating, setAnimating] = useState(false);
  const [muted, setMuted] = useState(false);
  const [soundEnabled, setSoundEnabled] = useState(true);
  const [chatOpen, setChatOpen] = useState(false);
  const [playersOpen, setPlayersOpen] = useState(false);
  const [chatText, setChatText] = useState("");
  const [chatMessages, setChatMessages] = useState<Array<{id:string;name:string;text:string;at:number}>>([]);
  const [roomCode, setRoomCode] = useState("W100NB");
  const [coins] = useState(2450);

  useEffect(() => { try { const saved = JSON.parse(localStorage.getItem("ludo-settings") || "{}"); if (saved.sound !== undefined) setSoundEnabled(saved.sound !== false); } catch {} }, []);
  const diceTimer = useRef<number | null>(null);
  const revisionRef = useRef(-1);

  useEffect(() => { if (params.get("room")) setRoomCode(params.get("room") || "W100NB"); }, [params]);

  const players = useMemo(() => {
    if (game?.players?.length) return game.players.map((p) => String(p.playerId) === String(me) ? { ...p, name: p.name || myName, level: Number(p.level) > 0 ? Number(p.level) : myLevel, avatar: p.avatar || myAvatarUrl } : p);
    return myName ? [{ playerId: me || "1", name: myName, level: myLevel, avatar: myAvatarUrl, seat: 0 }] : [];
  }, [game?.players, me, myName, myAvatarUrl, myLevel]);

  const mine = players.find((p) => String(p.playerId) === String(me)) || players[0];
  const opponent = players.find((p) => String(p.playerId) !== String(mine?.playerId));
  const myColors = useMemo<Color[]>(() => mine?.colors?.length ? mine.colors : (playerColorsForSeats(players.length === 2 ? 2 : 4, mine?.seat ?? 0) as Color[]), [mine, players.length]);
  const myTurn = game ? game.currentPlayerId === me : true;
  const legalTokenKeys = useMemo(() => pending === null || !myTurn ? [] : tokens.filter((t) => myColors.includes(t.color) && canMove(tokens, t, pending)).map((t) => `${t.color}:${t.id}`), [pending, myTurn, tokens, myColors]);
  const applyState = useCallback((next: GameState) => { const r = Number(next.stateRevision ?? -1); if (r >= 0 && revisionRef.current >= 0 && r < revisionRef.current) return false; if (r >= 0) revisionRef.current = r; setGame(next); setTokens(normalizeTokens(next.tokens || {})); return true; }, []);

  useEffect(() => {
    let mounted = true; let localSocket: Socket | null = null;
    const connect = async () => {
      let playerId = ""; let profileName = ""; let profileAvatar = ""; let profileLevel = 1;
      try {
        const [authResponse, customizationResponse] = await Promise.all([fetch("/api/auth", { cache: "no-store" }), fetch("/api/customization", { cache: "no-store" })]);
        const d = await authResponse.json(); const customization = customizationResponse.ok ? await customizationResponse.json() : null;
        playerId = String(d?.user?.id || ""); profileName = String(d?.user?.username || "");
        profileAvatar = String(d?.user?.avatar || d?.user?.image || "");
        profileLevel = Math.max(1, Number(d?.user?.level) || 1);
        const equippedId = String(customization?.equippedAvatar || "");
        const equipped = Array.isArray(customization?.avatars) ? customization.avatars.find((a: {id?: string}) => a.id === equippedId) : null;
        if (equipped?.imageUrl || equipped?.icon) profileAvatar = String(equipped.imageUrl || equipped.icon);
      } catch {}
      if (!mounted) return;
      if (playerId) setMe(playerId); if (profileName) setMyName(profileName); setMyLevel(profileLevel); if (profileAvatar) setMyAvatarUrl(profileAvatar);
      const room = params.get("room") || roomCode; const roomSize = Number(params.get("size") || 2);
      localSocket = io(window.location.origin, { transports: ["websocket", "polling"], reconnection: true }); setSocket(localSocket);
      localSocket.on("connect", () => { if (room && playerId) localSocket?.emit("join-room", { roomCode: room, name: profileName, avatar: profileAvatar, roomSize, playerId }); });
      localSocket.on("roster", (members: Player[]) => { setGame((g) => (g ? { ...g, players: members } : { status: "waiting", currentPlayerId: null, dice: null, pendingMove: null, players: members, tokens: {} })); });
      localSocket.on("game-dice", (e: { value: DiceValue }) => { setRoll(e.value); setRemoteRolling(true); if (diceTimer.current) window.clearTimeout(diceTimer.current); diceTimer.current = window.setTimeout(() => setRemoteRolling(false), 900); });
      localSocket.on("chat", (message: { id?: string; name?: string; text?: string; at?: number }) => { if (!mounted || !message?.text) return; const id = String(message.id || ""); const name = String(message.name || "Player"); const text = String(message.text); const at = Number(message.at || Date.now()); setChatMessages((items) => { if (id && items.some((item) => item.id === id)) return items; const duplicateLocal = items.find((item) => item.id.startsWith("local-") && item.name === name && item.text === text && Math.abs(item.at - at) < 5000); if (duplicateLocal) return items; return [...items.slice(-99), { id: id || `remote-${at}`, name, text, at }]; }); });
      localSocket.on("game-state", (next: GameState) => { if (!mounted || !applyState(next)) return; if (next.dice !== null) setRoll(next.dice); setPending(next.currentPlayerId === playerId ? next.pendingMove : null); });
    };
    void connect();
    return () => { mounted = false; if (diceTimer.current) window.clearTimeout(diceTimer.current); localSocket?.disconnect(); };
  }, [applyState, roomCode, params]);

  const chooseToken = useCallback((color: Color, id: number) => { if (!socket || !game || !myTurn || pending === null || animating) return; const token = tokens.find((t) => t.color === color && t.id === id); if (!token || !myColors.includes(color) || !canMove(tokens, token, pending)) return; const target = nextProgress(token.position, pending); if (target === null) return; setPending(null); setAnimating(true); socket.emit("game-move", { tokenId: `${color}:${id}`, to: target }); }, [socket, game, myTurn, pending, animating, tokens, myColors]);
  const handleRoll = useCallback(() => { if (!socket || !game) { setRoll((Math.floor(Math.random() * 6) + 1) as DiceValue); return; } if (!myTurn || pending !== null || animating || remoteRolling) return; socket.emit("game-roll"); }, [socket, game, myTurn, pending, animating, remoteRolling]);
  const sendQuickReaction = (text: string) => { setChatText(text); setChatOpen(true); };
  const sendChatMessage = () => { const text = chatText.trim(); if (!text || !socket?.connected) return; const now = Date.now(); setChatMessages((items) => [...items.slice(-99), { id: `local-${now}`, name: mine?.name || myName || "Player", text, at: now }]); socket.emit("chat", { text }); setChatText(""); };
  const toggleSound = () => { const next = !soundEnabled; setSoundEnabled(next); try { const saved = JSON.parse(localStorage.getItem("ludo-settings") || "{}"); localStorage.setItem("ludo-settings", JSON.stringify({ ...saved, sound: next })); } catch {} window.dispatchEvent(new CustomEvent("ludo-settings-updated", { detail: { sound: next } })); };
  const leaveRoom = () => { if (!window.confirm("Are you sure you want to leave this match?")) return; try { localStorage.removeItem("ludo-room"); } catch {} if (!socket?.connected) { window.location.href = "/lobby"; return; } const go = () => { window.location.href = "/lobby"; }; socket.once("room-left", go); socket.emit("leave-room"); window.setTimeout(go, 900); };

  return (
    <main className="ludo-live-wrapper"><div className="ludo-live-container">
      <header className="multiplayer-topbar">
        <div className="multiplayer-player-card multiplayer-player-card-you"><div className="multiplayer-player-avatar"><PlayerAvatar src={mine?.avatar} fallback="👤" /></div><div className="multiplayer-player-copy"><div className="multiplayer-player-name-row"><strong>{mine?.name || myName || ""}</strong><span className="multiplayer-you-badge">YOU</span></div><div className="multiplayer-player-status multiplayer-player-status-you"><span className="multiplayer-status-dot" />{myTurn ? "Your Turn" : "Waiting"}</div></div><span className="multiplayer-level-badge">★ {mine?.level || myLevel}</span></div>
        <div className="multiplayer-topbar-logo" aria-label="Ludo Live"><span className="multiplayer-logo-crown">♛</span><strong>LUDO</strong><span>LIVE</span></div>
        <div className="multiplayer-player-card multiplayer-player-card-opponent"><div className="multiplayer-player-avatar"><PlayerAvatar src={opponent?.avatar} fallback="👤" /></div><div className="multiplayer-player-copy"><div className="multiplayer-player-name-row"><strong>{opponent?.name || ""}</strong></div><div className="multiplayer-player-status multiplayer-player-status-opponent"><span className="multiplayer-status-dot" />IN MATCH</div></div><span className="multiplayer-level-badge">★ {opponent?.level || 1}</span></div>
      </header>
      <div className="ll-board-stage"><div className="ll-board-frame"><LudoBoard theme={theme} demoTokens={tokens} onTokenClick={chooseToken} legalTokenKeys={legalTokenKeys} animateUpdates finishSound /></div></div>
      <div className="ll-bottom-panel"><div className="ll-controls-row">
        <div className="ll-user-box"><div className="ll-user-header"><div className="ll-user-avatar"><PlayerAvatar src={mine?.avatar} fallback="👤" /></div><div className="ll-user-copy"><b className="ll-u-name">{mine?.name || myName || ""}</b><div className="ll-u-level">★ {mine?.level || myLevel}</div></div><span className="ll-edit-mark">✎</span></div><div className="ll-coins-pill"><span className="coin-icon">🟡</span><b>{coins.toLocaleString()}</b></div></div>
        <div className="ll-dice-box"><div className="ll-turn-copy"><div className="ll-turn-title"><span className="dot green" /> YOUR TURN</div><div className="ll-turn-sub">Roll the dice and<br />make your move</div><div className="ll-dice-result">{roll}</div><div className="ll-dice-hint">Tap the dice to roll</div></div><div className="ll-dice-slot"><DemoDice value={roll} onRoll={handleRoll} disabled={!myTurn || pending !== null || animating || remoteRolling} botRolling={remoteRolling} /></div></div>
        <div className="ll-side-actions"><button type="button" className="ll-action-btn" onClick={() => setChatOpen((v) => !v)} aria-expanded={chatOpen}><span className="action-icon">•••</span><span>Chat</span></button><div className="ll-action-btn ll-voice-host"><ChatVoice roomCode={roomCode} playerId={me} members={players.map((p) => ({ id: p.playerId, playerId: p.playerId, name: p.name, online: true }))} /></div></div>
      </div>
      <div className="ll-reactions-bar">{QUICK_REACTIONS.map((text, idx) => <button key={idx} type="button" className="ll-pill-btn" onClick={() => sendQuickReaction(text)}>{text}</button>)}</div>
      <footer className="ll-footer"><button type="button" className="ll-foot-btn exit" onClick={leaveRoom}>↪ Leave Match</button><button type="button" className="ll-foot-btn" onClick={() => setPlayersOpen((v) => !v)}>👥 Players</button><button type="button" className="ll-foot-btn" onClick={toggleSound}>{soundEnabled ? "🔊 Sound" : "🔇 Sound"}</button><div className="ll-room-chip"><span className="shield">🛡️</span><small>Room ID: {roomCode}</small><span className="copy-icon">▢</span></div></footer>
      {chatOpen && <section className="mp-overlay-panel" role="dialog" aria-label="Room chat"><div className="mp-panel-head"><strong>Room Chat</strong><button type="button" onClick={() => setChatOpen(false)} aria-label="Close chat">×</button></div><div className="mp-chat-list">{chatMessages.length ? chatMessages.map((m) => <div className={`mp-chat-message ${m.id.startsWith("local-") || m.name === mine?.name ? "mine" : ""}`} key={`${m.id}-${m.at}`}><b>{m.name}</b><span>{m.text}</span></div>) : <p className="mp-empty">No messages yet. Say hello!</p>}</div><form className="mp-chat-compose" onSubmit={(e) => { e.preventDefault(); sendChatMessage(); }}><input value={chatText} onChange={(e) => setChatText(e.target.value)} maxLength={240} placeholder="Type a message…" aria-label="Chat message"/><button type="submit">Send</button></form></section>}
      {playersOpen && <section className="mp-overlay-panel mp-players-panel" role="dialog" aria-label="Players in room"><div className="mp-panel-head"><strong>Players in Room</strong><button type="button" onClick={() => setPlayersOpen(false)} aria-label="Close players">×</button></div><div className="mp-player-list">{players.map((p) => <div className="mp-player-row" key={p.playerId}><div className="mp-mini-avatar"><PlayerAvatar src={p.avatar} fallback="👤" /></div><div><b>{p.name}</b><small>{String(p.playerId) === String(me) ? "You" : "In match"}</small></div></div>)}</div></section>}
      </div>
      <style jsx global>{`html,body{margin:0!important;padding:0!important;width:100%;height:100%;overflow:hidden!important;background:#000!important;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif}*{box-sizing:border-box}.ludo-live-wrapper{position:fixed;inset:0;display:flex;justify-content:center;align-items:center;background:#000;overflow:hidden}.ludo-live-container{position:relative;width:100%;max-width:none;height:100dvh;background:#000;display:grid;grid-template-rows:auto minmax(0,1fr) auto;padding:20px 22px 18px;overflow:hidden}.multiplayer-topbar{position:relative;display:grid;grid-template-columns:minmax(0,1fr) 150px minmax(0,1fr);align-items:center;gap:12px;width:100%;min-height:116px;padding:4px 0 14px;z-index:50}.multiplayer-player-card{position:relative;min-width:0;height:92px;display:flex;align-items:center;gap:12px;padding:10px 16px 10px 12px;border:1px solid rgba(102,76,25,.9);border-radius:31px;background:linear-gradient(145deg,#130f08,#060604);box-shadow:inset 0 1px 0 rgba(255,255,255,.04),0 10px 25px rgba(0,0,0,.5)}.multiplayer-player-avatar{width:66px;height:66px;flex:0 0 66px;border-radius:50%;overflow:hidden;display:grid;place-items:center;background:#0a0906;border:3px solid #d4af37;font-size:27px}.multiplayer-player-avatar img{width:100%;height:100%;object-fit:cover}.multiplayer-player-copy{min-width:0;flex:1}.multiplayer-player-name-row{display:flex;align-items:center;gap:8px}.multiplayer-player-name-row strong{color:#f7f3ea;font-size:clamp(13px,1.7vw,19px);font-weight:950;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.multiplayer-you-badge{padding:4px 8px;border-radius:7px;background:linear-gradient(#ffd85a,#e9a817);color:#181105;font-size:10px;font-weight:1000}.multiplayer-player-status{display:flex;align-items:center;gap:9px;margin-top:10px;font-size:clamp(11px,1.45vw,16px);font-weight:850}.multiplayer-player-status-you{color:#22e875}.multiplayer-player-status-opponent{color:#ff3347}.multiplayer-status-dot{width:16px;height:16px;border-radius:50%}.multiplayer-player-status-you .multiplayer-status-dot{background:#16e76b;box-shadow:0 0 12px #16e76b}.multiplayer-player-status-opponent .multiplayer-status-dot{background:#ff263e;box-shadow:0 0 12px #ff263e}.multiplayer-level-badge{position:absolute;left:58px;bottom:-13px;min-width:60px;padding:5px 9px;border:1px solid #b48722;border-radius:15px;background:#090806;color:#f1ca51;font-size:13px;font-weight:950;text-align:center}.multiplayer-topbar-logo{width:132px;height:104px;justify-self:center;display:flex;flex-direction:column;align-items:center;justify-content:center;color:#f5cf61;text-align:center;filter:drop-shadow(0 0 12px rgba(212,175,55,.35))}.multiplayer-logo-crown{font-size:33px;line-height:.7}.multiplayer-topbar-logo strong{font-family:Georgia,serif;font-size:32px;line-height:.9;color:#f7d56d;text-shadow:0 2px #6d4500}.multiplayer-topbar-logo>span:last-child{font-size:19px;font-weight:1000;letter-spacing:1px;color:#e9b52e}.multiplayer-menu-btn span{width:36px;height:5px;border-radius:99px;background:#e0a916}.mp-overlay-panel{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:min(92%,420px);max-height:70%;display:flex;flex-direction:column;background:linear-gradient(145deg,#171109,#070706);border:1px solid rgba(212,175,55,.55);border-radius:20px;box-shadow:0 18px 60px rgba(0,0,0,.8),0 0 30px rgba(212,175,55,.12);z-index:100;padding:14px;color:#f4e6b1}.mp-panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:16px;padding-bottom:10px;border-bottom:1px solid rgba(212,175,55,.2)}.mp-panel-head button{border:0;background:transparent;color:#f1d878;font-size:26px;cursor:pointer}.mp-chat-list{flex:1;min-height:120px;overflow:auto;padding:10px 2px;display:flex;flex-direction:column;gap:8px}.mp-chat-message{align-self:flex-start;max-width:85%;display:flex;flex-direction:column;gap:3px;padding:8px 10px;border-radius:12px;background:#20170b;border:1px solid rgba(212,175,55,.18)}.mp-chat-message.mine{align-self:flex-end;background:#12351e}.mp-chat-message b{font-size:10px;color:#e5bd4d}.mp-chat-message span{font-size:13px;color:#f7f3ea;word-break:break-word}.mp-empty{margin:auto;color:#a79b82;font-size:12px}.mp-chat-compose{display:flex;gap:7px;border-top:1px solid rgba(212,175,55,.2);padding-top:10px}.mp-chat-compose input{flex:1;min-width:0;border:1px solid rgba(212,175,55,.35);border-radius:10px;background:#080705;color:#fff;padding:9px}.mp-chat-compose button{border:1px solid #b88d28;border-radius:10px;background:#c7951c;color:#100d06;font-weight:900;padding:0 12px}.mp-player-list{overflow:auto;padding-top:10px;display:flex;flex-direction:column;gap:8px}.mp-player-row{display:flex;align-items:center;gap:10px;padding:9px;border-radius:12px;background:#100d08;border:1px solid rgba(212,175,55,.15)}.mp-player-row b{display:block;font-size:13px}.mp-player-row small{display:block;margin-top:2px;color:#76ef9e;font-size:10px}.mp-mini-avatar{width:40px;height:40px;border-radius:50%;overflow:hidden;border:2px solid #b88d28;display:grid;place-items:center;background:#090806}.mp-mini-avatar img{width:100%;height:100%;object-fit:cover}.mp-players-panel{max-height:55%}.ll-voice-host{padding:0!important;overflow:hidden}.ll-voice-host>div,.ll-voice-host button{width:100%!important;height:100%!important}.ll-voice-host button{border:0!important;border-radius:16px!important;font:inherit!important}.ll-board-stage{min-height:0;width:100%;height:100%;display:flex;align-items:center;justify-content:center;overflow:hidden;padding:2px 0 12px}.ll-board-frame{width:min(100%,calc(100dvh - 290px));max-width:100%;max-height:100%;height:auto;margin-inline:auto;aspect-ratio:1/1;padding:7px;border-radius:30px;background:linear-gradient(145deg,#f6da82 0%,#8d6819 43%,#e7c970 100%);box-shadow:0 14px 38px rgba(0,0,0,.8),0 0 0 1px rgba(212,175,55,.35);flex:0 1 auto}.ll-board-frame>div{width:100%!important;height:100%!important;aspect-ratio:1/1;border-radius:23px;overflow:hidden}.ll-bottom-panel{flex-shrink:0;display:flex;flex-direction:column;gap:9px;width:100%}.ll-controls-row{display:grid;grid-template-columns:minmax(175px,.82fr) minmax(300px,1.5fr) 82px;gap:10px;min-height:156px}.ll-user-box,.ll-dice-box,.ll-action-btn{background:linear-gradient(145deg,rgba(20,16,10,.98),rgba(7,7,5,.98));border:1px solid rgba(80,62,27,.72);box-shadow:inset 0 1px 0 rgba(255,255,255,.035),0 10px 24px rgba(0,0,0,.42)}.ll-user-box{min-width:0;border-radius:19px;padding:14px;display:flex;flex-direction:column;justify-content:space-between}.ll-user-header{position:relative;display:flex;align-items:center;gap:10px;min-width:0}.ll-user-avatar{width:52px;height:52px;flex:0 0 52px;border-radius:50%;background:#21190e;border:1.5px solid #d4af37;display:grid;place-items:center;font-size:22px;overflow:hidden}.ll-user-copy{min-width:0}.ll-u-name{font-size:15px;color:#fff;display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ll-u-level{font-size:12px;color:#d4af37;margin-top:3px;font-weight:850}.ll-edit-mark{margin-left:auto;color:#d4af37;font-size:16px}.ll-coins-pill{display:flex;align-items:center;gap:8px;min-height:43px;background:#090806;border:1px solid #302513;border-radius:22px;padding:5px 8px 5px 11px}.coin-icon{font-size:19px;line-height:1}.ll-coins-pill b{font-size:16px;color:#fff}.ll-dice-box{min-width:0;border-radius:19px;padding:12px 15px;position:relative;overflow:hidden;display:flex;align-items:stretch;justify-content:space-between}.ll-turn-copy{min-width:155px;padding:6px 0 0 4px;display:flex;flex-direction:column;align-items:flex-start}.ll-turn-title{font-size:16px;font-weight:950;color:#39e87a;display:flex;align-items:center;gap:7px}.ll-turn-title .dot{width:10px;height:10px;border-radius:50%;background:#4ade80;box-shadow:0 0 10px #4ade80}.ll-turn-sub{font-size:12px;line-height:1.35;color:#747474;margin-top:5px}.ll-dice-result{width:108px;height:42px;margin-top:auto;border-radius:21px;border:1px solid #392c16;background:linear-gradient(180deg,#171207,#090805);display:grid;place-items:center;color:#fff;font-size:25px;font-weight:950;box-shadow:inset 0 1px 8px rgba(0,0,0,.5)}.ll-dice-hint{font-size:10px;color:#555;margin-top:4px;width:108px;text-align:center}.ll-dice-slot{position:relative;flex:1;min-width:130px;display:flex;align-items:center;justify-content:center}.ll-dice-slot :global(.dice-area){min-width:150px!important;transform:scale(.93);transform-origin:center center}.ll-dice-slot :global(.dice-button){width:138px!important;height:124px!important}.ll-side-actions{display:flex;flex-direction:column;gap:9px}.ll-action-btn{width:82px;height:calc((100% - 9px)/2);min-height:70px;border-radius:18px;color:#fff;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:6px;font-size:12px;cursor:pointer}.ll-action-btn span:last-child{font-size:12px;color:#d0c6ae;font-weight:750}.action-icon{font-size:20px!important;color:#f2dfaa!important;letter-spacing:1px}.ll-action-btn.off{border-color:rgba(239,68,68,.5)}.ll-reactions-bar{display:flex;gap:9px;overflow-x:auto;scrollbar-width:none}.ll-reactions-bar::-webkit-scrollbar{display:none}.ll-pill-btn{flex:0 0 auto;background:linear-gradient(145deg,#17130b,#090805);border:1px solid #3a2d16;color:#f0d477;border-radius:18px;padding:10px 18px;font-size:12px;font-weight:800;white-space:nowrap;cursor:pointer}.ll-footer{display:grid;grid-template-columns:auto auto auto minmax(170px,1fr);align-items:center;gap:10px}.ll-foot-btn,.ll-room-chip{min-height:43px;background:linear-gradient(145deg,#17130b,#090805);border:1px solid #3a2d16;color:#cfc6b0;border-radius:17px;padding:9px 16px;font-size:12px;font-weight:800;cursor:pointer}.ll-foot-btn.exit{color:#ef5555;border-color:rgba(239,68,68,.35)}.ll-room-chip{display:flex;align-items:center;justify-content:center;gap:7px;color:#8e8e8e;cursor:default;min-width:0}.ll-room-chip small{font-size:11px;color:#858585;font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ll-room-chip .shield{font-size:13px;color:#58d17c}.copy-icon{color:#d4af37;font-size:15px}@media(max-width:700px){.ludo-live-container{padding:14px 12px 12px}.multiplayer-topbar{min-height:58px;padding-bottom:9px;gap:8px}.multiplayer-topbar-mark{width:36px;height:36px;flex-basis:36px;border-radius:11px;font-size:17px}.multiplayer-topbar-title{gap:8px}.multiplayer-topbar-title strong{font-size:14px}.multiplayer-topbar-title span:not(.multiplayer-topbar-mark){font-size:7px;margin-top:2px;letter-spacing:.6px}.multiplayer-topbar-room{padding:7px 9px;border-radius:12px;gap:5px}.multiplayer-topbar-room span{font-size:7px}.multiplayer-topbar-room b{font-size:9px}.ll-board-frame{width:min(100%,calc(100dvh - 240px));max-height:100%}.ll-board-frame>div{border-radius:18px}.ll-controls-row{grid-template-columns:minmax(135px,.85fr) minmax(0,1.5fr) 58px;gap:6px;min-height:116px}.ll-user-box{padding:9px;border-radius:14px}.ll-user-avatar{width:38px;height:38px;flex-basis:38px;font-size:17px}.ll-u-name{font-size:10px}.ll-u-level{font-size:8px}.ll-edit-mark{font-size:12px}.ll-coins-pill{min-height:32px;padding:3px 5px 3px 7px;gap:4px}.coin-icon{font-size:14px}.ll-coins-pill b{font-size:11px}.ll-dice-box{padding:8px;border-radius:14px}.ll-turn-copy{min-width:90px;padding:3px 0 0}.ll-turn-title{font-size:10px;gap:4px}.ll-turn-title .dot{width:7px;height:7px}.ll-turn-sub{font-size:8px}.ll-dice-result{width:66px;height:29px;border-radius:15px;font-size:17px}.ll-dice-hint{width:66px;font-size:7px}.ll-dice-slot{min-width:72px}.ll-dice-slot :global(.dice-area){transform:scale(.55)}.ll-action-btn{width:58px;min-height:52px;border-radius:13px;gap:2px}.action-icon{font-size:15px!important}.ll-action-btn span:last-child{font-size:8px}.ll-side-actions{gap:6px}.ll-reactions-bar{gap:5px}.ll-pill-btn{padding:7px 11px;font-size:9px;border-radius:13px}.ll-footer{grid-template-columns:auto auto auto minmax(0,1fr);gap:5px}.ll-foot-btn,.ll-room-chip{min-height:34px;padding:6px 8px;font-size:8px;border-radius:11px}.ll-room-chip small{font-size:7px}.shield,.copy-icon{font-size:9px}}@media(max-width:700px){.multiplayer-topbar{grid-template-columns:minmax(0,1fr) 82px minmax(0,1fr);gap:5px;min-height:78px;padding-bottom:8px}.multiplayer-player-card{height:62px;gap:5px;padding:5px 7px;border-radius:20px}.multiplayer-player-avatar{width:43px;height:43px;flex-basis:43px;border-width:2px;font-size:18px}.multiplayer-player-name-row{gap:3px}.multiplayer-player-name-row strong{font-size:9px}.multiplayer-you-badge{padding:2px 4px;font-size:6px}.multiplayer-player-status{gap:4px;margin-top:4px;font-size:7px}.multiplayer-status-dot{width:7px;height:7px}.multiplayer-level-badge{left:34px;bottom:-9px;min-width:36px;padding:2px 5px;font-size:8px}.multiplayer-topbar-logo{width:78px;height:68px}.multiplayer-logo-crown{font-size:20px}.multiplayer-topbar-logo strong{font-size:21px}.multiplayer-topbar-logo>span:last-child{font-size:11px}.multiplayer-menu-btn span{width:23px;height:3px}}@media(max-height:760px){.ludo-live-container{padding-top:8px;padding-bottom:8px}.multiplayer-topbar{min-height:52px;padding-bottom:5px}.ll-board-frame{width:min(100%,calc(100dvh - 300px));max-height:100%}.ll-controls-row{min-height:100px}}`}</style>
    </main>
  );
}

export default function OnlineGamePage() {
  return <Suspense fallback={<div style={{ background: "#000", height: "100vh" }} />}><GameContent /></Suspense>;
}
