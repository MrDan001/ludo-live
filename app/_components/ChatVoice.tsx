"use client";
import {useEffect,useRef,useState} from "react";
import {io,type Socket} from "socket.io-client";

type Member={id:string;playerId?:string;name:string;role?:string;online?:boolean;peerId?:string};
declare global{interface Window{Peer?:any}}
function loadPeer(){return new Promise<any>((resolve,reject)=>{if(window.Peer)return resolve(window.Peer);const s=document.createElement("script");s.src="https://unpkg.com/peerjs@1.5.4/dist/peerjs.min.js";s.onload=()=>resolve(window.Peer);s.onerror=reject;document.head.appendChild(s)})}
const safe=(v:string)=>String(v||"").replace(/[^a-zA-Z0-9_-]/g,"").slice(0,32);
export default function ChatVoice({roomCode,playerId,members}:{roomCode:string;playerId:string;members:Member[]}){
 const peerRef=useRef<any>(null),streamRef=useRef<MediaStream|null>(null),connections=useRef<Record<string,any>>({}),peerPlayers=useRef<Record<string,string>>({}),peerNames=useRef<Record<string,string>>({}),activityTimer=useRef<number|null>(null),reconnectTimer=useRef<number|null>(null),socketRef=useRef<Socket|null>(null),selfPeer=useRef("");
 const[ready,setReady]=useState(false),[mic,setMic]=useState(false),[notice,setNotice]=useState(""); const selfPeerId=`ludo-chat-${safe(roomCode)}-${safe(playerId)}`;
 const setAvatarSpeaking=(pid:string,on:boolean)=>{const name=peerNames.current[pid]||members.find(m=>String(m.playerId||m.id)===String(pid))?.name;if(!name)return;document.querySelectorAll<HTMLButtonElement>(".ig-avatar").forEach(b=>{if(b.getAttribute("aria-label")===name)b.classList.toggle("voice-speaking",on)})};
 const broadcastSpeaking=(on:boolean)=>Object.values(connections.current).forEach((c:any)=>{if(c?.data?.open)try{c.data.send({type:"speaking",playerId:String(playerId),speaking:on})}catch{}});
 const playRemote=(peerId:string,s:MediaStream)=>{let a=document.getElementById(`chat-remote-${peerId}`) as HTMLAudioElement|null;if(!a){a=document.createElement("audio");a.id=`chat-remote-${peerId}`;a.autoplay=true;a.setAttribute("playsinline","true");a.style.display="none";document.body.appendChild(a)}a.srcObject=s;a.muted=false;void a.play().catch(()=>setNotice("Tap Mic once to allow voice playback."))};
 const removePeer=(id:string)=>{const a=document.getElementById(`chat-remote-${id}`) as HTMLAudioElement|null;if(a)a.remove();setAvatarSpeaking(peerPlayers.current[id]||id,false);delete connections.current[id];delete peerNames.current[id];delete peerPlayers.current[id]};
 const callPeer=(id:string,pid:string,name:string)=>{if(!streamRef.current||!peerRef.current||id===selfPeer.current)return;if(streamRef.current.getAudioTracks().every(t=>!t.enabled)||connections.current[id]?.voiceCall)return;peerNames.current[id]=name||"Player";peerPlayers.current[id]=pid||"";try{const call=peerRef.current.call(id,streamRef.current);if(call){connections.current[id]={...(connections.current[id]||{}),voiceCall:call};call.on("stream",(s:MediaStream)=>playRemote(id,s));call.on("close",()=>removePeer(id));call.on("error",()=>{})}}catch{}};
 const connectPeer=(id:string,name:string,pid:string)=>{if(!id||id===selfPeer.current||!peerRef.current)return;peerNames.current[id]=name||"Player";peerPlayers.current[id]=pid||"";if(connections.current[id]?.data?.open||connections.current[id]?.connecting)return;try{const c=peerRef.current.connect(id,{reliable:true});connections.current[id]={...(connections.current[id]||{}),data:c,connecting:true};c.on("open",()=>{connections.current[id]={...(connections.current[id]||{}),data:c,connecting:false};if(streamRef.current)callPeer(id,pid,name)});c.on("data",(m:any)=>{if(m?.type==="speaking")setAvatarSpeaking(String(m.playerId),!!m.speaking)});c.on("close",()=>removePeer(id));c.on("error",()=>delete connections.current[id])}catch{}};
 const connectMembers=()=>members.forEach(m=>{const pid=String(m.playerId||m.id||"");if(!pid||pid===String(playerId))return;connectPeer(String(m.peerId||`ludo-chat-${safe(roomCode)}-${safe(pid)}`),m.name||"Player",pid)});
 useEffect(()=>{if(!playerId)return;const s=io(window.location.origin,{transports:["websocket","polling"],reconnection:true});socketRef.current=s;s.on("connect",()=>{if(selfPeer.current)s.emit("set-peer-id",selfPeer.current)});s.on("roster",()=>{if(mic)connectMembers()});return()=>{s.disconnect();socketRef.current=null}},[roomCode,playerId,mic]);
 useEffect(()=>{connectMembers()},[members,ready,mic,roomCode,playerId]);
 useEffect(()=>{let dead=false;const start=async()=>{try{const PeerClass=await loadPeer();if(dead)return;const peer=new PeerClass(selfPeerId);peerRef.current=peer;selfPeer.current=selfPeerId;peer.on("open",()=>{if(!dead){setReady(true);socketRef.current?.emit("set-peer-id",selfPeerId);if(mic)connectMembers()}});peer.on("call",(call:any)=>{const prefix=`ludo-chat-${safe(roomCode)}-`;const pid=call.peer.startsWith(prefix)?call.peer.slice(prefix.length):call.peer;const member=members.find(m=>safe(String(m.playerId||m.id))===safe(pid));if(member){peerNames.current[call.peer]=member.name||"Player";peerPlayers.current[call.peer]=String(member.playerId||member.id||"")}if(streamRef.current)try{call.answer(streamRef.current)}catch{}call.on("stream",(s:MediaStream)=>playRemote(call.peer,s));call.on("close",()=>removePeer(call.peer));call.on("error",()=>{})});peer.on("error",(e:any)=>{console.warn("chat voice peer",e);if(mic&&!dead)setNotice(e?.type==="unavailable-id"?"Voice is reconnecting…":"Voice connection failed. Tap Mic to retry.")});peer.on("disconnected",()=>{if(!dead&&mic){setReady(false);if(reconnectTimer.current)window.clearTimeout(reconnectTimer.current);reconnectTimer.current=window.setTimeout(()=>{try{peer.reconnect()}catch{}},600)}})}catch(e){console.warn("voice init",e);if(mic&&!dead)setNotice("Voice chat could not start.")}};start();return()=>{dead=true;setReady(false);if(activityTimer.current)window.clearInterval(activityTimer.current);if(reconnectTimer.current)window.clearTimeout(reconnectTimer.current);broadcastSpeaking(false);Object.values(connections.current).forEach((x:any)=>{try{x.data?.close?.();x.voiceCall?.close?.()}catch{}});connections.current={};if(peerRef.current){try{peerRef.current.destroy()}catch{}}peerRef.current=null;streamRef.current?.getTracks().forEach(t=>t.stop());streamRef.current=null;document.querySelectorAll("audio[id^='chat-remote-']").forEach(a=>a.remove());document.querySelectorAll(".ig-avatar.voice-speaking").forEach(e=>e.classList.remove("voice-speaking"))}},[roomCode,playerId,selfPeerId]);
 const toggle=async()=>{if(!mic){try{if(!window.isSecureContext||!navigator.mediaDevices?.getUserMedia){setNotice("Microphone requires a secure HTTPS connection.");return}if(!peerRef.current||!ready){setNotice("Voice is still connecting. Try again in a moment.");return}const stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});streamRef.current=stream;setMic(true);setNotice("");socketRef.current?.emit("set-peer-id",selfPeerId);connectMembers();const Ctx=window.AudioContext||(window as any).webkitAudioContext;if(Ctx){const ctx=new Ctx(),src=ctx.createMediaStreamSource(stream),an=ctx.createAnalyser();an.fftSize=512;src.connect(an);const data=new Uint8Array(an.fftSize);let last=false;activityTimer.current=window.setInterval(()=>{an.getByteTimeDomainData(data);let sum=0;for(let i=0;i<data.length;i++){const v=(data[i]-128)/128;sum+=v*v}const rms=Math.sqrt(sum/data.length),on=rms>0.055;if(on!==last){last=on;setAvatarSpeaking(String(playerId),on);broadcastSpeaking(on)}},90)}}catch(e){console.warn("microphone",e);setNotice("Microphone permission was not granted. Allow microphone access in your browser settings and try again.")}}else{if(activityTimer.current)window.clearInterval(activityTimer.current);activityTimer.current=null;setAvatarSpeaking(String(playerId),false);broadcastSpeaking(false);streamRef.current?.getTracks().forEach(t=>t.stop());streamRef.current=null;setMic(false);setNotice("")}};
 return <div style={{display:"flex",alignItems:"center",justifyContent:"center",width:"100%",height:"100%",position:"relative"}}><button type="button" onClick={toggle} aria-pressed={mic} title={notice||"Toggle microphone"} style={{border:"1px solid rgba(221,187,81,.48)",borderRadius:16,padding:"8px 10px",width:"100%",height:"100%",background:mic?"linear-gradient(145deg,#154b2a,#092010)":"linear-gradient(145deg,#211706,#090806)",color:mic?"#62f49a":"#f1d878",fontWeight:900,cursor:"pointer",fontSize:10}}>{mic?"🎙 Mic On":"🎙 Mic Off"}</button>{notice&&<span className="voice-notice" style={{position:"absolute",bottom:"calc(100% + 8px)",right:0,zIndex:20,maxWidth:220,padding:"8px 10px",borderRadius:10,background:"#180d03",border:"1px solid rgba(215,185,74,.45)",color:"#f3df9a",fontSize:10,lineHeight:1.35,boxShadow:"0 8px 24px rgba(0,0,0,.35)"}}>{notice}</span>}<style jsx global>{`.ig-avatar.voice-speaking{border-color:#f5d76e!important;box-shadow:0 0 0 2px rgba(245,215,110,.72),0 0 16px rgba(245,190,45,.95),0 0 30px rgba(245,190,45,.55)!important;animation:ludoGoldVoicePulse 900ms ease-in-out infinite}.ig-avatar.voice-speaking:after{content:"";position:absolute;inset:-5px;border:2px solid rgba(245,210,90,.5);border-radius:50%;animation:ludoGoldVoiceRing 900ms ease-out infinite;pointer-events:none}@keyframes ludoGoldVoicePulse{0%,100%{transform:scale(1);filter:brightness(1)}50%{transform:scale(1.08);filter:brightness(1.25)}}@keyframes ludoGoldVoiceRing{0%{transform:scale(.92);opacity:.85}100%{transform:scale(1.25);opacity:0}}`}</style></div>;
}
