"use client";

import { useEffect, useRef, useState } from "react";
import { io, type Socket } from "socket.io-client";
import ChatVoice from "./ChatVoice";
import PlayerIdentityLink from "./PlayerIdentityLink";
import AvatarRenderer from "./AvatarRenderer";
import { AVATAR_ICONS } from "./EquippedAvatar";

type Member={id:string;playerId?:string;name:string;host?:boolean;ready?:boolean;board?:string;dice?:string;connected?:boolean;avatar?:string};
type Msg={id:string;name:string;text:string;at:number;type?:string};
type AvatarProfile={username:string;equipped:{avatar:string}};
type StakeType="free"|"paid";
const QUICK=["👋 Hi!","😂 LOL","🔥 Nice!","😮 Wow!","👏 Good move","🎉 GG","❤️","😎"];
const avatarVisual=(id?:string,imageUrl?:string|null)=><AvatarRenderer avatar={{id:String(id||"default"),icon:AVATAR_ICONS[String(id||"default")]||"🧑🏽‍🎮",imageUrl:imageUrl||null}} size={52} border="3px solid #d7b94a" background="#0b2a62" fallback={AVATAR_ICONS[String(id||"default")]||"🧑🏽‍🎮"}/>;
const CSS=`
.live-social{width:100%;color:#fff}.room-hero{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:0 4px 8px}.room-hero small{display:block;color:#64748b;font-size:10px;letter-spacing:1px}.room-hero strong{display:block;font-size:clamp(24px,7vw,34px);letter-spacing:4px}.room-copy,.room-chat-close{border:1px solid #334155;background:#0f172a;color:#fff;border-radius:12px;padding:8px 10px;font-weight:800;cursor:pointer}.room-stage{padding:10px;border-radius:18px;background:#030817;border:1px solid rgba(148,163,184,.16)}.room-stage-head{display:flex;justify-content:space-between;padding:0 4px 7px;font-size:16px}.room-stage-head span{color:#94a3b8}.room-player-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}.room-player-card,.room-empty{min-height:112px;padding:8px;border-radius:14px;border:1px solid rgba(59,130,246,.18);background:linear-gradient(160deg,#081a42,#071127);display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;position:relative}.room-player-card.is-host{border-color:rgba(250,204,21,.4)}.room-avatar-wrap{position:relative;margin-bottom:3px;width:58px;height:58px}.room-avatar{width:52px;height:52px;box-sizing:border-box;border-radius:50%;display:flex;align-items:center;justify-content:center;background:#0b2a62;border:3px solid #d7b94a;overflow:hidden}.room-crown{position:absolute;left:-2px;top:-9px;font-size:22px;z-index:2}.room-player-card strong{font-size:14px;color:#f8fafc;max-width:100%;overflow:hidden;text-overflow:ellipsis}.room-status{margin-top:2px;font-weight:900;font-size:11px}.room-status.ready{color:#4ade80}.room-status.not-ready{color:#f87171}.room-kick{margin-top:3px;border:0;border-radius:7px;padding:3px 6px;background:#7f1d1d;color:#fff;font-size:10px;font-weight:900}.room-empty{cursor:pointer;color:#64748b;border-style:dashed}.room-empty span{font-size:34px;line-height:1;color:#60a5fa}.room-empty small{margin-top:3px;font-size:10px}.room-controls{display:grid;grid-template-columns:auto auto 1fr auto;gap:6px;align-items:center;margin-top:8px}.room-controls>button,.room-controls>div button{min-height:42px}.room-ready,.room-start,.room-chat-jump{border:0;border-radius:12px;padding:8px 9px;font-size:12px;font-weight:900;cursor:pointer;touch-action:manipulation}.room-ready{background:#2563eb;color:#fff}.room-ready.is-ready{background:#16a34a}.room-start{background:#16a34a;color:#fff}.room-start:disabled{opacity:.45;cursor:not-allowed}.room-chat-jump{background:#0f172a;color:#fff;border:1px solid #334155;font-size:20px}.room-voice{min-height:42px}.live-social-notice{display:block;margin-top:8px}.room-chat-drawer{position:fixed;inset:0;z-index:1000;background:rgba(2,6,23,.72);display:flex;align-items:flex-end;justify-content:center;padding:12px}.room-chat-sheet{width:min(760px,100%);max-height:78vh;border:1px solid rgba(96,165,250,.28);border-radius:22px 22px 14px 14px;background:#071127;padding:16px;display:flex;flex-direction:column}.room-chat-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px}.room-chat-messages{min-height:140px;max-height:42vh;overflow:auto;padding:10px 2px}.room-chat-empty{display:block;color:#94a3b8;padding:30px 0}.room-chat-message{padding:7px 0;color:#e2e8f0}.room-chat-quick{display:flex;gap:7px;flex-wrap:wrap;padding:8px 0}.room-chat-quick button{border:1px solid #334155;background:#0f172a;color:#e2e8f0;border-radius:14px;padding:7px 10px}.room-chat-composer{display:flex;gap:8px}.room-chat-input{flex:1;border:1px solid #334155;background:#111b30;color:#fff;border-radius:12px;padding:12px}.room-chat-send{border:0;border-radius:12px;background:#16a34a;color:#fff;font-weight:900;padding:0 16px}@media(max-width:380px){.room-player-card,.room-empty{min-height:98px}.room-avatar-wrap{width:50px;height:50px}.room-avatar{width:46px;height:46px}.room-controls{grid-template-columns:1fr 1fr auto}.room-controls .room-start{grid-column:1/-1}}
`;

export default function LiveSocial({roomCode,name,host=false,roomSize=4,stakeType="free",stakeCoins=0,compact=false,onStart,onKicked,leaveRequested=false,onLeaveComplete}:{roomCode:string;name:string;host?:boolean;roomSize?:number;stakeType?:StakeType;stakeCoins?:number;compact?:boolean;onStart?:()=>void;onKicked?:()=>void;leaveRequested?:boolean;onLeaveComplete?:()=>void}){
 const socketRef=useRef<Socket|null>(null);const startRef=useRef(onStart);const kickRef=useRef(onKicked);const leaveRef=useRef(onLeaveComplete);startRef.current=onStart;kickRef.current=onKicked;leaveRef.current=onLeaveComplete;
 const[members,setMembers]=useState<Member[]>([]);const[messages,setMessages]=useState<Msg[]>([]);const[text,setText]=useState("");const[notice,setNotice]=useState("");const[selfId,setSelfId]=useState("");const[selfPlayerId,setSelfPlayerId]=useState("");const[profiles,setProfiles]=useState<Record<string,AvatarProfile>>({});const[avatarImages,setAvatarImages]=useState<Record<string,string>>({});const[chatOpen,setChatOpen]=useState(false);
 const enrich=(list:Member[])=>{const names=[...new Set(list.map(m=>m.name).filter(Boolean))];Promise.all(names.map(async username=>{try{const r=await fetch(`/api/player/${encodeURIComponent(username)}`,{cache:"no-store"});if(!r.ok)return null;const d=await r.json();return [username,{username,equipped:d.player?.equipped||{avatar:"default"}}] as const}catch{return null}})).then(rows=>{const ok=rows.filter(Boolean) as [string,AvatarProfile][];if(!ok.length)return;setProfiles(p=>{const n={...p};for(const x of ok)n[x[0]]=x[1];return n});setMembers(prev=>prev.map(m=>{const p=ok.find(x=>x[0]===m.name)?.[1];return p?{...m,avatar:p.equipped.avatar||m.avatar}:m}))}).catch(()=>{})};
 useEffect(()=>{let cancelled=false;const connect=async()=>{let playerId="",board="classic",dice="classic";try{const a=await fetch("/api/auth",{cache:"no-store"}).then(r=>r.json());playerId=String(a.user?.id||"");board=a.user?.equippedBoard||board;dice=a.user?.equippedDice||dice}catch{}setSelfPlayerId(playerId);try{const c=await fetch("/api/customization",{cache:"no-store"}).then(r=>r.json());board=c.equippedBoard||board;dice=c.equippedDice||dice}catch{}try{const c=await fetch("/api/shop/catalog",{cache:"no-store"}).then(r=>r.json());const map:Record<string,string>={};for(const item of Array.isArray(c?.items)?c.items:[])if(item?.type==="avatar"&&item?.id&&item?.imageUrl)map[String(item.id)]=String(item.imageUrl);if(!cancelled)setAvatarImages(map)}catch{}if(cancelled)return;const socket=io(window.location.origin,{transports:["websocket","polling"],reconnection:true});socketRef.current=socket;socket.on("connect",()=>{setSelfId(socket.id||"");socket.emit("join-room",{roomCode,name,roomSize,playerId,board,dice,host,stakeType,stakeCoins:Number(stakeCoins)||0})});socket.on("roster",(list:Member[])=>{setMembers(list);enrich(list);const me=list.find(m=>m.id===socket.id||m.playerId===playerId);if(me)setSelfPlayerId(me.playerId||playerId)});socket.on("chat",(m:Msg)=>setMessages(x=>[...x,m].slice(-80)));socket.on("start-game",(payload:any)=>{try{localStorage.setItem("ludo-match-board",payload?.board||"classic");localStorage.setItem("ludo-match-members",JSON.stringify(payload?.members||[]))}catch{}setNotice("Game starting…");startRef.current?.()});socket.on("room-error",(m:string)=>setNotice(m||"Unable to update the room."));socket.on("start-error",(m:string)=>setNotice(m||"Unable to start the game."));socket.on("kicked",()=>{setNotice("You were removed by the room host.");kickRef.current?.()});socket.on("room-left",()=>{socket.disconnect();socketRef.current=null;leaveRef.current?.()});return()=>{cancelled=true;socket.disconnect();socketRef.current=null}};connect();return()=>{cancelled=true}},[roomCode,name,host,roomSize,stakeType,stakeCoins]);
 useEffect(()=>{if(!leaveRequested)return;const socket=socketRef.current;if(!socket){leaveRef.current?.();return}setNotice("Leaving room…");let done=false;const timer=window.setTimeout(()=>{if(done)return;done=true;socket.disconnect();socketRef.current=null;leaveRef.current?.()},1000);socket.emit("leave-room");return()=>window.clearTimeout(timer)},[leaveRequested]);
 const self=members.find(m=>m.id===selfId||m.playerId===selfPlayerId);const ready=!!self?.ready;const serverHost=!!members.find(m=>(m.host&&m.playerId===selfPlayerId)||(m.host&&m.id===selfId));const canStart=host&&serverHost&&members.length===roomSize&&members.every(m=>m.ready&&m.connected!==false);
 const send=(value=text)=>{const v=value.trim();if(!v)return;socketRef.current?.emit("chat",{text:v});setText("")};const toggleReady=()=>{if(!socketRef.current?.connected){setNotice("Reconnecting to the room…");return}socketRef.current.emit("ready",{ready:!ready})};const kick=(id:string)=>socketRef.current?.emit("kick-player",id);const startGame=()=>{if(!socketRef.current?.connected||!canStart)return;setNotice("Starting game…");socketRef.current.emit("start-game")};const voiceMembers=members.filter(m=>m.playerId).map(m=>({id:String(m.playerId),name:m.name,role:m.host?"owner":"member",online:m.connected!==false}));const slots=Array.from({length:roomSize},(_,i)=>members[i]||null);
 return <section className={`live-social${compact?" live-social-compact":""}`}><style dangerouslySetInnerHTML={{__html:CSS}}/><div className="room-hero"><div><small>ROOM ID</small><strong>{roomCode}</strong></div><button type="button" onClick={()=>navigator.clipboard?.writeText(roomCode)} className="room-copy">📋 Copy code</button></div><div className="room-stage"><div className="room-stage-head"><b>Players</b><span>{members.length}/{roomSize}</span></div><div className="room-player-grid">{slots.map((m,i)=>m?<div key={m.id} className={`room-player-card${m.host?" is-host":""}`}><div className="room-avatar-wrap">{m.host&&<span className="room-crown">👑</span>}<PlayerIdentityLink username={m.name}><div className="room-avatar">{avatarVisual(profiles[m.name]?.equipped?.avatar||m.avatar,avatarImages[profiles[m.name]?.equipped?.avatar||m.avatar||""])}</div></PlayerIdentityLink></div><PlayerIdentityLink username={m.name}><strong>{m.name}{(m.id===selfId||m.playerId===selfPlayerId)?" (you)":""}</strong></PlayerIdentityLink><span className={`room-status ${m.ready?"ready":"not-ready"}`}>{m.ready?"Ready":"Not Ready"}</span>{host&&serverHost&&!m.host&&<button type="button" onClick={()=>kick(m.id)} className="room-kick">Kick</button>}</div>:<button key={`empty-${i}`} type="button" className="room-empty" onClick={()=>navigator.clipboard?.writeText(roomCode)}><span>＋</span><small>Invite player</small></button>)}</div><div className="room-controls"><div className="room-voice"><ChatVoice socket={socketRef.current} roomCode={roomCode} playerId={selfPlayerId} members={voiceMembers}/></div><button type="button" onClick={toggleReady} className={`room-ready${ready?" is-ready":""}`}>{ready?"✓ Ready":"Ready"}</button>{host&&serverHost&&<button type="button" onClick={startGame} disabled={!canStart} className="room-start">{canStart?"START GAME":"WAITING"}</button>}<button type="button" onClick={()=>setChatOpen(true)} className="room-chat-jump">💬</button></div>{notice&&<div className="live-social-notice">{notice}</div>}</div>{chatOpen&&<div className="room-chat-drawer" onClick={()=>setChatOpen(false)}><div className="room-chat-sheet" onClick={e=>e.stopPropagation()}><div className="room-chat-top"><b>Room chat</b><button className="room-chat-close" onClick={()=>setChatOpen(false)}>Close</button></div><div className="room-chat-messages">{messages.length===0?<span className="room-chat-empty">No messages yet. Chat with the other players here.</span>:messages.map((m,i)=><div key={m.id+"-"+i} className="room-chat-message"><b>{m.name}:</b> {m.text}</div>)}</div><div className="room-chat-quick">{QUICK.map(q=><button key={q} type="button" onClick={()=>send(q)}>{q}</button>)}</div><div className="room-chat-composer"><input className="room-chat-input" value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")send()}} placeholder="Type a message…"/><button type="button" className="room-chat-send" onClick={()=>send()}>Send</button></div></div></div>}</section>;
}
