"use client";
import { useEffect, useMemo, useState } from "react";
import type { CSSProperties } from "react";
import AppFrame from "../_components/AppFrame";
type Stats={gameRoomsEntered:number;spinsUsed:number;messagesSent:number;chatRoomsJoined:number;chatRoomsCreated:number};type Wallet={coins:number;gems:number;spins:number;mystery:number};type Mission={id:string;title:string;target:number;rewardCoins:number;value:number};
const defaults:Stats={gameRoomsEntered:0,spinsUsed:0,messagesSent:0,chatRoomsJoined:0,chatRoomsCreated:0};const walletDefault:Wallet={coins:25680,gems:320,spins:0,mystery:0};
function readStats():Stats{try{return {...defaults,...JSON.parse(localStorage.getItem("ludo-stats")||"{}")}}catch{return defaults}}function readWallet():Wallet{try{return {...walletDefault,...JSON.parse(localStorage.getItem("ludo-wallet")||"{}")}}catch{return walletDefault}}function dayKey(){const d=new Date();return `${d.getFullYear()}-${d.getMonth()+1}-${d.getDate()}`}
export default function MissionsPage(){const [tab,setTab]=useState("Daily"),[stats,setStats]=useState<Stats>(defaults),[claimed,setClaimed]=useState<string[]>([]),[wallet,setWallet]=useState<Wallet>(walletDefault),[message,setMessage]=useState("");const refresh=()=>{setStats(readStats());setWallet(readWallet());try{const r=JSON.parse(localStorage.getItem("ludo-claimed-missions")||"{}");setClaimed(r.day===dayKey()?r.ids||[]:[])}catch{setClaimed([])}};useEffect(()=>{refresh();window.addEventListener("ludo-stats-updated",refresh);window.addEventListener("ludo-wallet-updated",refresh);window.addEventListener("storage",refresh);return()=>{window.removeEventListener("ludo-stats-updated",refresh);window.removeEventListener("ludo-wallet-updated",refresh);window.removeEventListener("storage",refresh)}},[]);const daily=useMemo<Mission[]>(()=>[{id:"games",title:"Win 3 matches",target:3,value:Math.min(3,stats.gameRoomsEntered),rewardCoins:1000},{id:"play5",title:"Play 5 matches",target:5,value:Math.min(5,stats.gameRoomsEntered),rewardCoins:500},{id:"four",title:"Win a match with 4 players",target:1,value:Math.min(1,Number(typeof window!=="undefined"?localStorage.getItem("ludo-four-player-wins")||0:0)),rewardCoins:1000},{id:"coins500",title:"Collect 500 coins",target:500,value:Math.min(500,Number(typeof window!=="undefined"?localStorage.getItem("ludo-coins-collected")||0:0)),rewardCoins:1000},{id:"spin2",title:"Use spin wheel 2 times",target:2,value:Math.min(2,stats.spinsUsed),rewardCoins:500}],[stats]);const claim=(m:Mission)=>{if(m.value<m.target||claimed.includes(m.id))return;const ids=[...claimed,m.id],w={...wallet,coins:wallet.coins+m.rewardCoins};localStorage.setItem("ludo-claimed-missions",JSON.stringify({day:dayKey(),ids}));localStorage.setItem("ludo-wallet",JSON.stringify(w));window.dispatchEvent(new Event("ludo-wallet-updated"));setClaimed(ids);setWallet(w);setMessage(`Reward added: ${m.rewardCoins.toLocaleString()} coins.`);setTimeout(()=>setMessage(""),2500)};const claimAll=()=>daily.filter(m=>m.value>=m.target&&!claimed.includes(m.id)).forEach(claim);return <AppFrame back=""><main style={page}><header style={header}><button onClick={()=>history.back()} style={back}>←</button><div style={headerTitle}>Missions</div><div/></header><div style={tabs}>{["Daily","Weekly","Achievements"].map(t=><button key={t} onClick={()=>setTab(t)} style={{...tabStyle,...(tab===t?tabActive:{})}}>{t}</button>)}</div>{tab==="Daily"?<section style={list}>{daily.map(m=>{const done=m.value>=m.target,percent=Math.min(100,Math.round(m.value/m.target*100));return <article key={m.id} style={card}><div style={{...status,background:done?"#20ad48":"#d99318"}}>{done?"✓":"×"}</div><div style={body}><div style={topRow}><strong>{m.title}</strong><span>🪙 {m.rewardCoins.toLocaleString()}</span></div><div style={track}><div style={{...bar,width:`${percent}%`,background:done?"#25b84e":"#1682ed"}}/></div><div style={bottomRow}><span>{m.value} / {m.target}</span>{done&&!claimed.includes(m.id)?<button onClick={()=>claim(m)} style={claimBtn}>Claim</button>:claimed.includes(m.id)?<span style={claimedText}>CLAIMED</span>:null}</div></div></article>})}</section>:<section style={empty}><div style={{fontSize:42}}>{tab==="Weekly"?"📅":"🏆"}</div><h2>{tab}</h2><p>{tab==="Weekly"?"Weekly missions will refresh with larger objectives and rewards.":"Achievements track your long-term Ludo progress."}</p></section>}{message&&<div style={messageBox}>✓ {message}</div>}<button onClick={claimAll} disabled={!daily.some(m=>m.value>=m.target&&!claimed.includes(m.id))} style={{...claimAllBtn,opacity:daily.some(m=>m.value>=m.target&&!claimed.includes(m.id))?1:.45}}>Claim All</button></main></AppFrame>}
const page:CSSProperties={maxWidth:650,margin:"0 auto",paddingBottom:45};const header:CSSProperties={height:52,display:"grid",gridTemplateColumns:"44px 1fr 44px",alignItems:"center",marginBottom:8};const back:CSSProperties={border:0,background:"transparent",color:"#fff",fontSize:32,cursor:"pointer",textAlign:"left",lineHeight:1};const headerTitle:CSSProperties={textAlign:"center",fontSize:20,fontWeight:950};const tabs:CSSProperties={display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:4,padding:4,borderRadius:9,background:"#06152b",border:"1px solid #12345c"};const tabStyle:CSSProperties={border:0,borderRadius:7,padding:"11px 3px",background:"#0a1d38",color:"#dbe8f8",fontSize:13,fontWeight:900,cursor:"pointer"};const tabActive:CSSProperties={background:"linear-gradient(180deg,#277cf0,#1762d1)",color:"#fff"};const list:CSSProperties={display:"grid",gap:5,marginTop:12};const card:CSSProperties={display:"flex",gap:12,alignItems:"center",padding:"12px 10px",background:"#06162d",borderBottom:"1px solid rgba(38,83,130,.35)"};const status:CSSProperties={flex:"0 0 30px",width:30,height:30,borderRadius:"50%",display:"grid",placeItems:"center",color:"#fff",fontSize:19,fontWeight:950};const body:CSSProperties={flex:1,minWidth:0};const topRow:CSSProperties={display:"flex",justifyContent:"space-between",alignItems:"center",gap:8,fontSize:14};const track:CSSProperties={height:7,borderRadius:99,background:"#102540",overflow:"hidden",marginTop:8,maxWidth:360};const bar:CSSProperties={height:"100%",borderRadius:99,transition:"width .25s"};const bottomRow:CSSProperties={display:"flex",justifyContent:"space-between",alignItems:"center",marginTop:6,color:"#91a5c0",fontSize:12};const claimBtn:CSSProperties={border:0,borderRadius:7,padding:"7px 22px",background:"linear-gradient(180deg,#57d928,#31a913)",color:"#fff",fontWeight:950,cursor:"pointer"};const claimedText:CSSProperties={color:"#5bd77c",fontWeight:950,fontSize:10};const claimAllBtn:CSSProperties={width:"100%",marginTop:14,border:0,borderRadius:7,padding:14,background:"linear-gradient(180deg,#4bd522,#269b11)",color:"#fff",fontWeight:950,fontSize:17};const empty:CSSProperties={marginTop:12,padding:38,background:"#07172d",borderRadius:12,border:"1px solid #17385f",textAlign:"center",color:"#9db0c8"};const messageBox:CSSProperties={marginTop:9,padding:10,borderRadius:8,background:"#07341b",border:"1px solid #208a4a",color:"#7ef0a4",fontSize:12,textAlign:"center"};
