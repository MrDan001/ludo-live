import { NextRequest, NextResponse } from "next/server";import { createHash, randomUUID } from "crypto";import { pool } from "../auth/_db";import { ensureEventsSchema, eventState } from "./_schema";import { ensureWalletAudit, markWalletContext } from "../lib/wallet-audit";
const COOKIE="ludo_session";async function currentUser(q:NextRequest){const t=q.cookies.get(COOKIE)?.value;if(!t)return null;const h=createHash("sha256").update(t).digest("hex"),r=await pool.query<any>(`SELECT u.id,u.username,u.email,u.is_guest,u.is_banned FROM ludo_users u JOIN ludo_sessions s ON s.user_id=u.id WHERE s.token_hash=$1 AND s.expires_at>NOW() LIMIT 1`,[h]),u=r.rows[0];return u&&!u.is_guest&&!u.is_banned?u:null}
async function visualSchema(){await pool.query(`ALTER TABLE ludo_events ADD COLUMN IF NOT EXISTS board_id TEXT NOT NULL DEFAULT 'classic';ALTER TABLE ludo_events ADD COLUMN IF NOT EXISTS dice_id TEXT NOT NULL DEFAULT 'classic';ALTER TABLE ludo_events ADD COLUMN IF NOT EXISTS yard_id TEXT NOT NULL DEFAULT 'yard-classic';ALTER TABLE ludo_events ADD COLUMN IF NOT EXISTS yard_kind TEXT NOT NULL DEFAULT 'background'`)}
async function settleExpiredEvents(){const c=await pool.connect();try{await c.query("BEGIN");await ensureWalletAudit(c);const es=await c.query<any>(`SELECT * FROM ludo_events WHERE status='published' AND ends_at<=NOW() FOR UPDATE`);for(const e of es.rows){await c.query(`UPDATE ludo_events SET status='ended',updated_at=NOW() WHERE id=$1`,[e.id]);const entries=await c.query<any>(`SELECT user_id FROM ludo_event_entries WHERE event_id=$1 AND completed=TRUE`,[e.id]);for(const x of entries.rows){const ins=await c.query(`INSERT INTO ludo_event_rewards(event_id,user_id,coins,gems) VALUES($1,$2,$3,$4) ON CONFLICT(event_id,user_id) DO NOTHING RETURNING event_id`,[e.id,x.user_id,Number(e.reward_coins)||0,Number(e.reward_gems)||0]);if(ins.rowCount){await markWalletContext(c,{source:"event_reward",sourceRef:`event:${e.id}`,requestId:randomUUID(),ip:"",userAgent:"",reason:`Event reward: ${e.title}`});await c.query(`UPDATE ludo_users SET coins=coins+$1,gems=gems+$2 WHERE id=$3`,[Number(e.reward_coins)||0,Number(e.reward_gems)||0,x.user_id]);await c.query(`UPDATE ludo_event_entries SET reward_claimed=TRUE WHERE event_id=$1 AND user_id=$2`,[e.id,x.user_id])}}await c.query(`UPDATE ludo_events SET settled_at=NOW(),updated_at=NOW() WHERE id=$1`,[e.id])}await c.query("COMMIT")}catch(e){await c.query("ROLLBACK");throw e}finally{c.release()}}
function present(r:any,joined=false){return{id:r.id,title:r.title,description:r.description,icon:r.icon,color:r.color,reward:r.reward,rewardCoins:Number(r.reward_coins),rewardGems:Number(r.reward_gems),eventType:r.event_type,missionKind:r.mission_kind,missionTarget:Number(r.mission_target),modes:r.modes||[],boards:r.boards||[],boardId:r.board_id||"classic",diceId:r.dice_id||"classic",yardId:r.yard_id||"yard-classic",yardKind:r.yard_kind||"background",startsAt:new Date(r.starts_at).toISOString(),endsAt:new Date(r.ends_at).toISOString(),state:eventState(r.starts_at,r.ends_at,r.status),joined,progress:Number(r.progress||0),completed:Boolean(r.completed),rewardClaimed:Boolean(r.reward_claimed),settledAt:r.settled_at?new Date(r.settled_at).toISOString():null}}
export async function GET(q:NextRequest){try{await ensureEventsSchema();await visualSchema();await settleExpiredEvents();const u=await currentUser(q),r=await pool.query<any>(`SELECT e.*,ee.user_id IS NOT NULL AS joined,COALESCE(ee.progress,0) progress,COALESCE(ee.completed,FALSE) completed,COALESCE(ee.reward_claimed,FALSE) reward_claimed FROM ludo_events e LEFT JOIN ludo_event_entries ee ON ee.event_id=e.id AND ee.user_id=$1 WHERE e.status<>'draft' AND e.status<>'cancelled' ORDER BY e.starts_at ASC`,[u?.id||null]);return NextResponse.json({events:r.rows.map((x:any)=>present(x,Boolean(x.joined))),serverNow:new Date().toISOString()})}catch(e){console.error("events GET",e);return NextResponse.json({error:"Events service unavailable."},{status:500})}}
export async function POST(q:NextRequest){try{await ensureEventsSchema();await visualSchema();await settleExpiredEvents();const u=await currentUser(q);if(!u)return NextResponse.json({error:"Login required."},{status:401});const b=await q.json(),action=String(b.action||"");if(action==="join"){const id=String(b.eventId||""),r=await pool.query<any>(`SELECT * FROM ludo_events WHERE id=$1 LIMIT 1`,[id]);if(!r.rowCount)return NextResponse.json({error:"Event not found."},{status:404});const e=r.rows[0],state=eventState(e.starts_at,e.ends_at,e.status);if(state!=="live")return NextResponse.json({error:state==="upcoming"?"This event is not live yet.":"This event has expired."},{status:409});await pool.query(`INSERT INTO ludo_event_entries(event_id,user_id) VALUES($1,$2) ON CONFLICT(event_id,user_id) DO NOTHING`,[id,u.id]);return NextResponse.json({ok:true,eventId:id,boardId:e.board_id||"classic",diceId:e.dice_id||"classic",yardId:e.yard_id||"yard-classic",yardKind:e.yard_kind||"background"})}if(action==="activity"){const kind=String(b.kind||""),amount=Math.max(1,Math.min(100,Math.trunc(Number(b.amount||1))));if(!kind)return NextResponse.json({error:"Activity kind required."},{status:400});const c=await pool.connect();try{await c.query("BEGIN");const r=await c.query<any>(`SELECT e.id,e.mission_target,e.mission_kind,ee.progress FROM ludo_event_entries ee JOIN ludo_events e ON e.id=ee.event_id WHERE ee.user_id=$1 AND e.status='published' AND e.starts_at<=NOW() AND e.ends_at>NOW() AND ee.completed=FALSE AND e.mission_kind=$2 FOR UPDATE`,[u.id,kind]);for(const e of r.rows){const next=Math.min(Number(e.mission_target),Number(e.progress)+amount),done=next>=Number(e.mission_target);await c.query(`UPDATE ludo_event_entries SET progress=$1,completed=$2,completed_at=CASE WHEN $2 THEN NOW() ELSE completed_at END WHERE event_id=$3 AND user_id=$4`,[next,done,e.id,u.id])}await c.query("COMMIT");return NextResponse.json({ok:true,updated:r.rowCount})}catch(e){await c.query("ROLLBACK");throw e}finally{c.release()}}return NextResponse.json({error:"Unknown event action."},{status:400})}catch(e){console.error("events POST",e);return NextResponse.json({error:"Event action failed."},{status:500})}}
