import { NextRequest,NextResponse } from "next/server";import {createHash,randomUUID} from "crypto";import {pool,ensureAuthSchema} from "../auth/_db";import {getShopCatalog} from "../shop/catalog";import {adjustWallet} from "../lib/wallet-audit";
const COOKIE="ludo_session",BROWSER_COOKIE="ludo_spin_browser";type SpinReward={id:string;kind:"coins"|"gems"|"extraSpin"|"shop_item";label:string;icon:string;amount:number;itemType?:string;itemId?:string;probability:number;active:boolean};
async function userId(request:NextRequest){const token=request.cookies.get(COOKIE)?.value;if(!token)return null;const hash=createHash("sha256").update(token).digest("hex"),r=await pool.query<{id:string}>("SELECT u.id FROM ludo_users u JOIN ludo_sessions s ON s.user_id=u.id WHERE s.token_hash=$1 AND s.expires_at>NOW() LIMIT 1",[hash]);return r.rows[0]?.id??null}function dayKey(){return new Date().toISOString().slice(0,10)}function browserKey(request:NextRequest){return request.cookies.get(BROWSER_COOKIE)?.value||randomUUID()}function withBrowserCookie(response:NextResponse,key:string,set:boolean){if(set)response.cookies.set(BROWSER_COOKIE,key,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:31536000});return response}
async function ensureTables(){await ensureAuthSchema();await pool.query(`CREATE TABLE IF NOT EXISTS ludo_spin_state(user_id TEXT PRIMARY KEY REFERENCES ludo_users(id) ON DELETE CASCADE,last_free_spin DATE,spins INTEGER NOT NULL DEFAULT 0,total_spins INTEGER NOT NULL DEFAULT 0)`);await pool.query(`CREATE TABLE IF NOT EXISTS ludo_spin_visitor_state(browser_key TEXT PRIMARY KEY,last_spin DATE,spins INTEGER NOT NULL DEFAULT 0,total_spins INTEGER NOT NULL DEFAULT 0)`);await pool.query("ALTER TABLE ludo_spin_state ADD COLUMN IF NOT EXISTS active_seconds INTEGER NOT NULL DEFAULT 0");await pool.query("ALTER TABLE ludo_spin_state ADD COLUMN IF NOT EXISTS last_heartbeat_at TIMESTAMPTZ");await pool.query(`CREATE TABLE IF NOT EXISTS ludo_spin_rewards(id TEXT PRIMARY KEY,kind TEXT NOT NULL CHECK(kind IN ('coins','gems','extraSpin','shop_item')),label TEXT NOT NULL,icon TEXT NOT NULL,amount INTEGER NOT NULL DEFAULT 0,item_type TEXT,item_id TEXT,probability NUMERIC NOT NULL DEFAULT 1,active BOOLEAN NOT NULL DEFAULT TRUE,sort_order INTEGER NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await pool.query(`CREATE TABLE IF NOT EXISTS ludo_spin_item_rewards(id BIGSERIAL PRIMARY KEY,user_id TEXT NOT NULL REFERENCES ludo_users(id) ON DELETE CASCADE,item_type TEXT NOT NULL,item_id TEXT NOT NULL,item_name TEXT NOT NULL,item_icon TEXT,won_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),claimed_at TIMESTAMPTZ)`);await pool.query(`CREATE INDEX IF NOT EXISTS ludo_spin_item_rewards_pending_idx ON ludo_spin_item_rewards(user_id,claimed_at,won_at DESC)`);
const defaults=[['coins1000','coins','1,000','🪙',1000,null,null,20,0],['gems5','gems','5','💎',5,null,null,15,1],['coins2000','coins','2,000','🪙',2000,null,null,15,2],['gems10','gems','10','💎',10,null,null,10,3],['coins5000','coins','5,000','🪙',5000,null,null,8,4],['gems15','gems','15','💎',15,null,null,7,5],['coins3000','coins','3,000','🪙',3000,null,null,15,6],['extraSpin','extraSpin','EXTRA SPIN','🔄',1,null,null,10,7]];
for(const x of defaults)await pool.query(`INSERT INTO ludo_spin_rewards(id,kind,label,icon,amount,item_type,item_id,probability,sort_order) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9) ON CONFLICT(id) DO NOTHING`,x);
}
async function rewards():Promise<SpinReward[]>{await ensureTables();const r=await pool.query<SpinReward>(`SELECT id,kind,label,icon,amount,item_type AS "itemType",item_id AS "itemId",probability,active FROM ludo_spin_rewards WHERE active=TRUE ORDER BY sort_order,id`);return r.rows.map(x=>({...x,amount:Number(x.amount),probability:Number(x.probability)}))}function weighted(list:SpinReward[]){const total=list.reduce((s,x)=>s+Math.max(0,Number(x.probability)||0),0);if(!total)return list[0];let n=Math.random()*total;for(const x of list){n-=Math.max(0,Number(x.probability)||0);if(n<=0)return x}return list[list.length-1]}const payload=(extra:any={})=>({serverTime:new Date().toISOString(),...extra});
export async function GET(request:NextRequest){try{await ensureTables();const id=await userId(request),wheel=await rewards();if(id){const r=await pool.query("SELECT spins,total_spins FROM ludo_spin_state WHERE user_id=$1",[id]),row=r.rows[0];return NextResponse.json(payload({spins:Number(row?.spins||0),totalSpins:Number(row?.total_spins||0),visitor:false,rewards:wheel}),{headers:{"Cache-Control":"no-store"})})}const publicWheel=wheel.filter(x=>x.kind!=="shop_item"),key=browserKey(request),r=await pool.query("SELECT last_spin,spins,total_spins FROM ludo_spin_visitor_state WHERE browser_key=$1",[key]),row=r.rows[0],ready=!row||!row.last_spin||String(row.last_spin).slice(0,10)!==dayKey(),response=NextResponse.json(payload({spins:ready?1:Number(row?.spins||0),totalSpins:Number(row?.total_spins||0),visitor:true,rewards:publicWheel}),{headers:{"Cache-Control":"no-store"}});return withBrowserCookie(response,key,!request.cookies.get(BROWSER_COOKIE)?.value)}catch(e){console.error(e);return NextResponse.json({error:"Spin wheel unavailable."},{status:500})}}
export async function POST(request:NextRequest){try{await ensureTables();const id=await userId(request),all=await rewards(),wheel=id?all:all.filter(x=>x.kind!=="shop_item"),prize=weighted(wheel);if(!prize)return NextResponse.json({error:"No spin rewards are configured."},{status:503});const prizeIndex=wheel.findIndex(x=>x.id===prize.id);if(id){const claimed=await pool.query("UPDATE ludo_spin_state SET spins=spins-1,total_spins=total_spins+1 WHERE user_id=$1 AND spins>0 RETURNING total_spins,spins",[id]);if(!claimed.rows[0])return NextResponse.json({error:"No free spin available. Stay active to earn another one."},{status:409});if(prize.kind==="coins"||prize.kind==="gems")await adjustWallet(pool,id,prize.kind,prize.amount,{source:"spin_wheel",sourceRef:prize.id,reason:`Spin Wheel reward: ${prize.label}`});else if(prize.kind==="extraSpin")await pool.query("UPDATE ludo_spin_state SET spins=spins+1 WHERE user_id=$1",[id]);else if(prize.kind==="shop_item"&&prize.itemType&&prize.itemId){const catalog=await getShopCatalog(),item:any=catalog.find((x:any)=>x.type===prize.itemType&&x.id===prize.itemId);if(!item)return NextResponse.json({error:"Configured Spin Shop item is no longer available."},{status:409});await pool.query(`INSERT INTO ludo_spin_item_rewards(user_id,item_type,item_id,item_name,item_icon) VALUES($1,$2,$3,$4,$5)`,[id,prize.itemType,prize.itemId,item.name,item.icon||prize.icon||"✨"])}const spins=Number(claimed.rows[0].spins)+(prize.kind==="extraSpin"?1:0);return NextResponse.json(payload({prize,prizeIndex,rewards:wheel,spins,totalSpins:Number(claimed.rows[0].total_spins),visitor:false}),{headers:{"Cache-Control":"no-store"}})}const key=browserKey(request),today=dayKey();await pool.query(`INSERT INTO ludo_spin_visitor_state(browser_key,last_spin,spins,total_spins) VALUES($1,NULL,0,0) ON CONFLICT(browser_key) DO NOTHING`,[key]);const claimed=await pool.query(`UPDATE ludo_spin_visitor_state SET last_spin=$2,spins=0,total_spins=total_spins+1 WHERE browser_key=$1 AND (last_spin IS NULL OR last_spin<>$2 OR spins>0) RETURNING total_spins`,[key,today]);if(!claimed.rows[0])return withBrowserCookie(NextResponse.json({error:"This browser has already used today's spin. Come back tomorrow."},{status:409}),key,!request.cookies.get(BROWSER_COOKIE)?.value);if(prize.kind==="extraSpin")await pool.query("UPDATE ludo_spin_visitor_state SET spins=1 WHERE browser_key=$1 AND last_spin=$2",[key,today]);const response=NextResponse.json(payload({prize,prizeIndex,rewards:wheel,spins:prize.kind==="extraSpin"?1:0,totalSpins:Number(claimed.rows[0].total_spins),visitor:true}),{headers:{"Cache-Control":"no-store"}});return withBrowserCookie(response,key,!request.cookies.get(BROWSER_COOKIE)?.value)}catch(e){console.error(e);return NextResponse.json({error:"Could not complete spin."},{status:500})}}
