import { NextRequest, NextResponse } from "next/server";
import { createHash, randomUUID } from "crypto";
import { pool, ensureAuthSchema } from "../../auth/_db";
const COOKIE="ludo_session"; const MIME=new Set(["image/png","image/webp"]); const MAX=4_000_000;
async function admin(q:NextRequest){const token=q.cookies.get(COOKIE)?.value;if(!token)return null;const h=createHash("sha256").update(token).digest("hex");const r=await pool.query<any>(`SELECT u.* FROM ludo_users u JOIN ludo_sessions s ON s.user_id=u.id WHERE s.token_hash=$1 AND s.expires_at>NOW() LIMIT 1`,[h]);const u=r.rows[0];if(!u||u.is_guest||u.is_banned)return null;const allowed=(process.env.ADMIN_EMAILS||process.env.ADMIN_EMAIL||"").split(",").map(x=>x.trim().toLowerCase()).filter(Boolean);return u.email&&allowed.includes(u.email.toLowerCase())?u:null;}
async function schema(){await ensureAuthSchema();await pool.query(`CREATE TABLE IF NOT EXISTS ludo_shop_yards(id TEXT PRIMARY KEY,name TEXT NOT NULL,description TEXT NOT NULL DEFAULT '',kind TEXT NOT NULL CHECK(kind IN ('background','sticker')),rarity TEXT NOT NULL DEFAULT 'COMMON',currency TEXT NOT NULL CHECK(currency IN ('coins','gems','naira')),price INTEGER NOT NULL CHECK(price>=0),is_published BOOLEAN NOT NULL DEFAULT TRUE,sort_order INTEGER NOT NULL DEFAULT 0,image_data BYTEA NOT NULL,image_type TEXT NOT NULL,created_by TEXT REFERENCES ludo_users(id) ON DELETE SET NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);}
function row(r:any){return {id:r.id,type:"item",name:r.name,description:r.description,kind:r.kind,rarity:r.rarity,currency:r.currency,price:Number(r.price),isPublished:Boolean(r.is_published),imageUrl:`/api/shop/yards/${encodeURIComponent(r.id)}/image?v=${encodeURIComponent(new Date(r.updated_at).getTime())}`,createdAt:r.created_at,updatedAt:r.updated_at};}
async function list(){const r=await pool.query(`SELECT * FROM ludo_shop_yards ORDER BY sort_order,created_at DESC`);return r.rows.map(row);}
async function image(file:File,kind:string){if(!MIME.has(file.type))throw Error("Yard artwork must be PNG or WebP.");if(file.size<1||file.size>MAX)throw Error("Artwork must be smaller than 4 MB.");const b=Buffer.from(await file.arrayBuffer());if(kind==="sticker"&&file.type!=="image/png")throw Error("Backgroundless stickers must be transparent PNG files.");if(kind==="sticker"&&!b.toString("binary").includes("\x89PNG"))throw Error("Invalid PNG artwork.");return b;}
export async function GET(q:NextRequest){try{if(!(await admin(q)))return NextResponse.json({error:"Admin access required."},{status:403});await schema();return NextResponse.json({ok:true,yards:await list()},{headers:{"Cache-Control":"no-store"}});}catch(e){console.error(e);return NextResponse.json({error:"Unable to load yards."},{status:500});}}
export async function POST(q:NextRequest){try{const a=await admin(q);if(!a)return NextResponse.json({error:"Admin access required."},{status:403});await schema();const f=await q.formData();const name=String(f.get("name")||"").trim(),description=String(f.get("description")||"").trim(),kind=String(f.get("kind")||"");const rarity=String(f.get("rarity")||"COMMON").toUpperCase(),currency=String(f.get("currency")||"coins"),price=Math.trunc(Number(f.get("price"))),file=f.get("image");if(!name||name.length>80)return NextResponse.json({error:"Name is required and must be 80 characters or fewer."},{status:400});if(!["background","sticker"].includes(kind))return NextResponse.json({error:"Choose Background Yard or Backgroundless Sticker."},{status:400});if(!["COMMON","RARE","EPIC","LEGENDARY"].includes(rarity)||!["coins","gems","naira"].includes(currency)||!Number.isFinite(price)||price<0)return NextResponse.json({error:"Invalid rarity, currency or price."},{status:400});if(!file||!(file instanceof File))return NextResponse.json({error:"Artwork upload is required."},{status:400});const bytes=await image(file,kind);const id=`yard-${kind}-${randomUUID()}`;const r=await pool.query(`INSERT INTO ludo_shop_yards(id,name,description,kind,rarity,currency,price,is_published,sort_order,image_data,image_type,created_by) VALUES($1,$2,$3,$4,$5,$6,$7,TRUE,(SELECT COALESCE(MAX(sort_order),-1)+1 FROM ludo_shop_yards),$8,$9,$10) RETURNING *`,[id,name,description,kind,rarity,currency,price,bytes,file.type,a.id]);await pool.query(`INSERT INTO ludo_admin_actions(admin_user_id,action,target_user_id,details) VALUES($1,'yard_create',NULL,$2)`,[a.id,JSON.stringify({id,name,kind,rarity,currency,price,fileType:file.type,fileBytes:file.size})]);return NextResponse.json({ok:true,yard:row(r.rows[0]),yards:await list()});}catch(e:any){console.error(e);return NextResponse.json({error:e?.message||"Unable to create yard."},{status:400});}}
export async function PATCH(q:NextRequest){try{const a=await admin(q);if(!a)return NextResponse.json({error:"Admin access required."},{status:403});await schema();const f=await q.formData();const id=String(f.get("id")||"");const old=(await pool.query(`SELECT * FROM ludo_shop_yards WHERE id=$1`,[id])).rows[0];if(!old)return NextResponse.json({error:"Yard not found."},{status:404});const name=String(f.get("name")??old.name).trim(),description=String(f.get("description")??old.description).trim(),rarity=String(f.get("rarity")??old.rarity).toUpperCase(),currency=String(f.get("currency")??old.currency),price=Math.trunc(Number(f.get("price")??old.price)),published=String(f.get("isPublished")??old.is_published)==="true",file=f.get("image");if(!name||!Number.isFinite(price)||price<0)return NextResponse.json({error:"Valid name and price are required."},{status:400});if(file instanceof File&&file.size){const bytes=await image(file,old.kind);await pool.query(`UPDATE ludo_shop_yards SET name=$2,description=$3,rarity=$4,currency=$5,price=$6,is_published=$7,image_data=$8,image_type=$9,updated_at=NOW() WHERE id=$1`,[id,name,description,rarity,currency,price,published,bytes,file.type]);}else await pool.query(`UPDATE ludo_shop_yards SET name=$2,description=$3,rarity=$4,currency=$5,price=$6,is_published=$7,updated_at=NOW() WHERE id=$1`,[id,name,description,rarity,currency,price,published]);await pool.query(`INSERT INTO ludo_admin_actions(admin_user_id,action,target_user_id,details) VALUES($1,'yard_update',NULL,$2)`,[a.id,JSON.stringify({id,name,kind:old.kind,replacedArtwork:Boolean(file&&file instanceof File&&file.size)})]);return NextResponse.json({ok:true,yards:await list()});}catch(e:any){console.error(e);return NextResponse.json({error:e?.message||"Unable to update yard."},{status:400});}}
