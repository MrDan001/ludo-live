# Ludo Live Admin

This is the dedicated administrator application service. It has its own origin, PWA manifest, service worker and install identity, while securely proxying the existing server-backed admin console and APIs.